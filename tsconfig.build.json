{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // No jest globals in the shipped build: `describe` or `expect` left in src/ should fail
    // the build rather than compile quietly.
    "types": ["node"]
  },
  "exclude": ["node_modules", "dist", "**/*.spec.ts", "**/*.fixtures.ts", "test"]
}
